version: "3.8"

services:
  ckan:
    build:
      dockerfile: Dockerfile.dev
    volumes:
      - ./src:/srv/app/src_extensions

  caddy:
    volumes:
      - ./caddy/Caddyfile.dev:/etc/caddy/Caddyfile

  db:
    env_file:
      - .env
    ports:
      - "5432:5432"

  solr:
    ports:
      - "8983:8983"
